import React from 'react';
import {connect} from "react-redux";

const AdministratorProfile = () => {
    //TODO - ADMINISTRATOR DASHBOARD + GRAFICE
    return (
        <React.Fragment>

        </React.Fragment>
    );
};

export default connect(null, null)(AdministratorProfile);
